package com.leesin.java8.me.design.model.策略;

public class LambdaProcess {
    public static void main(String[] args) {
        // 简单的方法直接lambda即可
        // List<Original> list = Arrays.asList((x, y) -> x + y, (x, y) -> x - y, (x, y) -> x * y);
        // list.forEach(i -> i.compute(1, 2));
    }
}
